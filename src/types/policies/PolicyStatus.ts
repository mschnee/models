// enum: ['created', 'uploaded', 'qa', 'ready', 'sent', 'declined', 'approved', 'in-progress', 'complete', 'canceled']
export enum PolicyStatus {
  Created = 'created',
  Uploaded = 'uploaded',
  Qa = 'qa',
  Ready = 'ready',
  Sent = 'sent',
  Declined = 'declined',
  Approved = 'approved',
  Complete = 'complete',
  Canceled = 'canceled',
  InProgress = 'in-progress',
}
